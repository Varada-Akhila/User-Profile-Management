Single-page React app to create, view, update, and delete user profiles with an interactive UI.Uses React hooks, Axios for real-time updates, and JSON Server as a mock backend.
             
how to run
==========

cd db
json-server --watch users.json --port 9090


cd front-end
npm install 
npm run dev
